/**
 * @note order of slides is controlled by the order of this enum keys/values.
 */
export enum SlideName {
  ActivatingSlide = 'ACTIVATING_SLIDE',
  ScrollingSlide = 'SCROLLING_SLIDE',
  ClickingSlide = 'CLICKING_SLIDE',
  GettingHelpSlide = 'GETTING_HELP_SLIDE'
}
